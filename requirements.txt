numpy>=1.23
pandas>=1.5
scikit-learn>=1.2
joblib>=1.2
matplotlib>=3.6
pyproj>=3.4  # optional; only required when using lon/lat input
pyarrow>=10  # optional; parquet output falls back to CSV if unavailable

# Optional, required only for raster-based environmental exposure diagnostics
rasterio
